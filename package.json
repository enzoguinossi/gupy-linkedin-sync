{
  "name": "gupy-sync",
  "version": "0.2.6",
  "description": "CLI para sincronizar dados do LinkedIn com a Gupy",
  "type": "module",

  "bin": {
    "gupy-sync": "dist/index.js"
  },

  "main": "dist/index.js",

  "files": [
    "dist",
    "README.md",
    "/docs",
    "LICENSE"
  ],

  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc",
    "prepack": "npm run build"
  },

  "dependencies": {
    "@inquirer/prompts": "^8.2.0",
    "axios": "^1.13.2",
    "axios-cookiejar-support": "^6.0.5",
    "commander": "^14.0.2",
    "dotenv": "^17.2.3",
    "tough-cookie": "^6.0.0",
    "udsv": "^0.7.3"
  },

  "devDependencies": {
    "typescript": "^5.9.3"
  },

  "engines": {
    "node": ">=18"
  }
}
